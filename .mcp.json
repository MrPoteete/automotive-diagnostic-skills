{
  "mcpServers": {
    "gemini": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "export GEMINI_API_KEY=$(gcloud secrets versions access latest --secret=gemini-api-key --project=poteete-secrets 2>/dev/null) && npx -y @houtini/gemini-mcp"
      ]
    }
  }
}
